// Swap Variables: Write a function that takes two numbers as input and swaps their values using array destructuring.

function swap (num1, num2) {
    let x = num2;
    let y = num1;

    console.log(`num1 is ${x}\nnum2 is ${y}`);
}

let num1 = 5;
let num2 = 8;
swap(num1, num2)