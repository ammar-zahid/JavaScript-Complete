// Write a program to iterate over an array of numbers using the for...of loop and print each number to the console.

const array = [1,2,5,6,8,9];

for (const number of array) {
    console.log(number);
}