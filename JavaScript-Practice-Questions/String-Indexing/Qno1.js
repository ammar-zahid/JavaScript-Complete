// Write a JavaScript function that takes a string as input and returns the character at the specified index.
// Example:
// Input: stringIndex('Hello', 2)
// Output: 'l'

const stringIndex = (str, index) => {
    return str[index];
}

console.log(stringIndex("Captain", 3));