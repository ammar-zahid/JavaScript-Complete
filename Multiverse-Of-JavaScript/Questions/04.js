// write a js function to convert an array elements into string.

function elements (array) {
    return array.toString();
}

const answer = elements(["red", "green", "blue", "yellow"])
console.log(answer)